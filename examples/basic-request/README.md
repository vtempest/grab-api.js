# grab-url · Basic Request

Minimal Vite + TypeScript example: a GET request, a GET with query params, and a POST, using [`grab-url`](https://github.com/vtempest/grab-url).

```bash
npm install
npm run dev
```
