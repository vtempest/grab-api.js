# Changelog

***

## September 2026 — Docs overhaul and runnable examples

The current month has been a concentrated push on the documentation site. **README and docs-site badges** were brought to parity with debate-ai.com, with **npm downloads**, **bundle size** and **grab.js.org** badges added (PRs #15, #16, #17). An **OpenAPI SDKs overview page** and matching homepage section were published (PR #18), followed by a **Hey API setup guide**.

The **examples page was migrated to runnable, StackBlitz-linked examples** so every snippet can be forked and executed in the browser (PRs #20, #21). The docs app's **routing and MDX config were synced with template-fumadocs**, moving the `docs-search`, `llms.mdx` and `llms-full.txt` routes under `app/docs/`.

An interactive **KineticGrid backdrop** was added to the homepage hero (PR #23), the deployment target was **switched from OpenNext/Cloudflare to plain Vercel-compatible Next.js** (PRs #22, #24, #25), and the loading-animations package gained **8 monochrome spinners with a live docs gallery** (PR #26). Finally, **all npm audit findings were cleared** and broken build tasks repaired (PR #27).

## August 2026 — OpenAPI SDKs

The month’s feature was **`heyapi-client-grab`**: an **OpenAPI SDK client powered by grab**, so a typed client generated by [Hey API](https://heyapi.dev) sends its requests through `grab()` instead of axios — every endpoint inheriting caching, retries, rate limiting, dedupe and mocks for free (PR #14).

Two fixes shipped alongside it:

- Mock keys now match with or without a leading slash.
- A broken npm install that was blocking CI was repaired.

## July 2026 — Coverage

**Codecov coverage reporting** was set up with Vitest and wired into CI (PR #13), giving the repo its coverage badge.

The **loading-animations package** also received improved configuration and explicit **licensing**.

## June 2026 — Slim builds and restructuring

The grab API was refactored with **slim build support** and **enhanced content processing**, while **response handling** was improved.

Repository URLs were corrected across every `package.json` to point at the GRAB-URL monorepo, using the `git+https` form npm requires. A CI lockfile bug was also fixed so the workflow only adds lock files that actually exist. Release **1.6.22** shipped.

Mid-month brought:

- A dedicated **grab-options documentation page** with expanded option coverage.
- A further restructuring of **`grab-api`** and its sibling packages for clarity.

## May 2026 — Quantum orbital, Cloudflare and package hygiene

The month opened with another **mobile horizontal-scroll fix** and the removal of the **Docs nav link** from the homepage header (PR #12), followed by an upgrade to **fumadocs-core 16.8.5**.

A round of **deduplication** removed redundant code and unused graph hierarchy logic.

The **quantum-sphere-loading-animation** package received substantial work:

- `QuantumWaveOrbital` was renamed **`QuantumOrbital`**.
- Its logic was extracted into specialized hooks and utility files.
- Sphere configuration was consolidated into shared modules so the **React and Svelte implementations share defaults**.
- Its CSS was inlined into the component.

The repo also gained **READMEs for every package**, standard workspace-wide **VS Code settings**, and **OpenNext Cloudflare deployment** configuration. **API2AI security features** were documented.

## April 2026 — Mobile fixes and a package that didn't stay

Two mobile issues were fixed on the 22nd:

- The docs hero header being cut off (PR #10).
- Horizontal scrolling cutting off the sidebar and navigation (PR #11).

Release **1.1.1** followed.

Late in the month, a **`git0-download-repo` package** was initialized with core logic, CLI tools, and Docusaurus docs. It was then **removed three days later**, along with its documentation references.

A **test script** and **universal asset classification** in `releases.ts` also landed.

## March 2026 — The 1.5 rewrite and the monorepo

This was the largest month in project history, with **80 commits**, and it reshaped the project structurally.

It opened on the 5th with the **1.5 major rewrite**:

- The single-file library was split into modular files.
- Full JSDoc was added.
- Documentation was regenerated from the codebase.
- Versions **1.5.1 through 1.5.5** shipped the same day.
- The **Claude Agent Skill**, `use-grab-request`, was published so agents can install grab’s usage rules with one command.
- Unit tests were wired into CI.
- An npm publishing workflow and new logo were added.

On the 6th, the repo became a **monorepo**, split into dedicated packages for:

- CLI
- API
- Logging
- Loading animations
- Web archiving

The rest of the month expanded the documentation platform:

- CLI aliases and a feature-comparison table.
- Filetree and badge-tooltip components.
- Interactive Mermaid diagrams with pan/zoom, node tooltips, and highlighting.
- A collapsible dependency graph.
- Tooltip, slider, popover, and markdown UI components with correct `forwardRef` handling.
- MDX responses fixed to serve as `text/plain`, so browsers display them instead of downloading them (PRs #6 and #7).
- LLM-friendly documentation routes.
- A replacement of the original file-tree generator with a code-graph analysis and visualization system.
- C4 diagram generation and import/export badges.
- Internal `@grab-url` packages bundled into Vite rather than externalized.
- Missing CLI dependencies — `chalk`, `cli-table3`, and `cli-progress` — repaired (PR #8).
- A mobile info icon added to badge tooltips for touch devices (PR #9).

Version **0.1.3** of the monorepo packages closed the month.

## December 2025 — Homepage rebuild and the Claude-authored PRs

December was the busiest month since launch, with **37 commits**.

Eight patch releases — **1.0.9 through 1.0.15** — shipped on the 13th, followed by three more — **1.0.16 through 1.0.18** — on the 26th.

The docs homepage was then **rebuilt from scratch**, accompanied by dependency and lockfile cleanup.

The 30th marked the repo’s first **AI-authored pull requests**:

- PRs #2–#5 added syntax highlighting to the docs.
- Syntax highlighting was applied across every homepage code tab.
- Fumadocs icon rendering was fixed.

Manual polish followed:

- Hero buttons.
- Code font family.
- The `Alt+` DevTools shortcut hint.
- Showcases for **mcp-use** and an **api2ai video**.

Releases **1.0.2 through 1.0.5** closed the year.

## September 2025 — 1.0

The project reached **version 1.0.4** on the 5th and **1.0.7** by the 15th, marking its first stable releases.

The site received a new logo and favicon, plus a **“copy for LLM”** button so the documentation could be copied wholesale into a chat assistant.

The rest of the month focused on **URL and favicon fixes**.

## August 2025 — Docs replatform

After a quiet July, the documentation site was migrated to **Fumadocs** with an automated build pipeline, replacing the earlier static setup.

The docs outline was reorganized, and a **build preview** was wired into git.

## June 2025 — Streaming, icons and the 0.9.x series

June turned the prototype into a publishable package.

- Infinite scroll was fixed.
- **Debounce** was added.
- Requests could regrab on network changes or window refocus.
- Scroll-position recovery landed.
- The icons package was made to export correctly with tooltip previews.
- **DevTools** became operational via `Ctrl+I`.

The headline feature was **`onStream` for streaming responses**, which went through several iterations on 9 June before stabilizing.

Request and response types could now be passed as parameters, giving developers error-checking and tooltips for their own API shapes.

Several compatibility and runtime fixes followed:

- No debug output by default.
- `globalThis` defaults for Node.
- A Node crash in `showAlert`.
- Axios-style `.data` compatibility.
- An `http:` URL handling bug.

Versions **0.9.112 through 0.9.127** were released in rapid succession. The month ended with **colored log output and a spinner**.

## May 2025 — The first 49 commits

The project was born on **26 May 2025** as `grab-api.js`, with a single initial commit already containing a fully functional **`grab()` function** and documentation.

Within days, it acquired much of the shape it still has:

- Bundling arrived via Vite alongside the first **20 Vitest unit tests**.
- The log store was renamed and converted to an array so request history could be inspected properly in the console.
- The feature list expanded to **15 points**.
- An `onBeforeCallback` hook was added.
- TypeScript types were introduced so editors could show tooltips describing every request option.

Documentation was a continuing focus:

- Docs moved repeatedly to make **GitHub Pages** build correctly.
- A **CNAME for js.org** was added and repeatedly corrected.
- The site received a favicon, logo, and screenshots.

The month concluded with two major content pushes:

- More than **10 substantial use-case examples**, a performance guide, and loading icons.
- A comparison table benchmarking grab against axios, SuperAgent, TanStack Query, Alova, SWR, and apisauce.

A proxy example, **instances**, and **repeat requests** landed on 29 May.
