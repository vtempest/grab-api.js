# grab-url · Reactive Loading (React)

A `UserProfile` component that hands its `useState` setter straight to grab's `response` option, so `isLoading`/`error`/data updates flow into React state automatically.

```bash
npm install
npm run dev
```
