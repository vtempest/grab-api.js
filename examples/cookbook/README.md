# grab-url · Cookbook

One runnable project covering debounced search, client-side caching, rate limiting, retry with error handling, request interceptors, and file upload — each wired to a real public demo API so it actually runs.

```bash
npm install
npm run dev
```
