---
title: Code Graph & Type Tree
icon: FolderTree
---

import { FileTreeView } from '@/components/fumadocs/code-tree/filetree-view';
import { InlineTOC } from 'fumadocs-ui/components/inline-toc';
import { docsConfig } from '@/lib/fumadocs/customize-docs';
import { DependencyGraph } from '@/components/fumadocs/codegraph/dependency-graph';

export const descriptions = {
  // grab-api
  "grab-api": "Core Grab API library",
  "grab-api/index.ts": "Main entry point. Exports the grab() function and log utility, sets up global instances, and initializes browser devtools.",
  "grab-api/common/types.ts": "All TypeScript definitions for request options, response shapes, and mock configurations.",
  "grab-api/common/utils.ts": "Low-level utilities like debouncer, wait (sleep), and buildUrl.",
  "grab-api/core/cache-pagination.ts": "Decides if a request should be served from cache and increments page numbers for infinite scrolling.",
  "grab-api/core/core.ts": "The heart of the library. Orchestrates the flow from option merging to final response mapping.",
  "grab-api/core/flow-control.ts": "Manages non-network request flow such as debouncing and repeating requests.",
  "grab-api/core/regrab-events.ts": "Sets up listeners (window focus, online status) to automatically refetch data.",
  "grab-api/core/request-executor.ts": "Handles the actual network fetch call or returns mock data if configured.",
  "grab-api/devtools/devtools.ts": "Implements a floating visual alert/notification system for the browser.",
  "grab-api/response/infinite-scroll.ts": "Logic for appending paginated results and saving/restoring scroll position in browsers.",
  "grab-api/response/response-handler.ts": "Transforms raw fetch responses into the predictable GrabResponse format.",
  // grab-url-cli
  "grab-url-cli": "Command-Line Interface",
  "grab-url-cli/index.ts": "Parses CLI arguments and switches between API Mode and Download Mode.",
  "grab-url-cli/cli-args.ts": "A custom, lightweight alternative to yargs for parsing flags and positional URLs.",
  "grab-url-cli/file-downloader.ts": "Orchestrator class that coordinates formatting, state, and keyboard modules for a unified download experience.",
  "grab-url-cli/keyboard-controls.ts": "Listens for keypresses (like 'p' for pause) during active downloads.",
  "grab-url-cli/download-spinners.ts": "Spinner animation configurations for download progress display.",
  "grab-url-cli/display/progress-format.ts": "Pure functions for converting bytes to human-readable strings, calculating ETAs, and building progress bars.",
  "grab-url-cli/display/spinner-config.ts": "Selects and animates randomized spinner frames for a polished CLI look.",
  "grab-url-cli/transfer/single-file-transfer.ts": "The driver logic for streaming a single file to disk with progress updates.",
  "grab-url-cli/transfer/multi-file-transfer.ts": "Manages a pool of concurrent downloads using a multi-progress bar display.",
  "grab-url-cli/transfer/resume-state.ts": "Handles .download-state sidecar files to allow resuming interrupted downloads.",
  // log-json
  "log-json": "Output formatting and logging",
  "log-json/colors.ts": "ANSI terminal colors.",
  "log-json/log-json.ts": "Robust logger that outputs colorized, structured JSON to the console.",
  "log-json/structure.ts": "TypeDoc-style JSON describer.",
  // loading-animations
  "loading-animations": "Assets & animations for CLI and browser",
  "loading-animations/cli/loading-animations-emojis.js": "A collection of character-based animation frames for terminal spinners.",
  "loading-animations/svg/index.ts": "Exports SVG animations as strings for use in grab-api devtools.",
  // archiver-web
  "archiver-web": "Web-based archive compression and extraction",
  "archiver-web/src/index.ts": "Main entry point for archive operations.",
  "archiver-web/src/types.ts": "TypeScript type definitions for archiver.",
  "archiver-web/src/bin-compress.ts": "CLI binary for compressing files into archives.",
  "archiver-web/src/bin-extract.ts": "CLI binary for extracting archive contents.",
  // open-when-ready
  "open-when-ready": "Opens browser when dev server is ready",
};

<InlineTOC items={toc} defaultOpen="true" />


## Code Dependency Graph

<DependencyGraph
  paths={["../packages"]}
  ghBase={docsConfig.githubPackages}
  descriptions={descriptions}
  ignore={["bun.lock"]}
  showLegend={true}
  showNpmImports={false}
  showTypes={false}
  showPrivateFunctions={false}
  showExportedFunctions={false} />

## Code Type Tree

<FileTreeView
  paths={["../packages"]}
  ghBase={docsConfig.githubPackages}
  descriptions={descriptions}
  ignore={["bun.lock"]}
  defaultImportFilter="all"
  defaultInternalFilter="all"
  defaultExportFilter="functions"
  defaultCollapseDepth={4}
/>

